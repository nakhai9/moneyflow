import instance from "./config";

export const API_SERVICES = {
    register: async () => {
        try {

        } catch (error) {
            console.log(error);
        }
    },
    login: async () => {
        try {

        } catch (error) {
            console.log(error);
        }
    }
}